<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Experiment Hub — Header</title>
  <style>
    :root{
      --bg0: #070a10;
      --bg1: #0b1020;
      --card: rgba(255,255,255,.06);
      --card2: rgba(255,255,255,.04);
      --stroke: rgba(255,255,255,.10);
      --text: rgba(255,255,255,.92);
      --muted: rgba(255,255,255,.70);
      --muted2: rgba(255,255,255,.55);
      --blue: rgba(0,180,255,.20);
      --blue2: rgba(0,120,255,.16);
      --glow: rgba(0,180,255,.22);
      --amber: rgba(255,140,0,.95);
      --amberGlow: rgba(255,140,0,.30);
      --radius: 18px;
    }

    * { box-sizing: border-box; }
    html, body { height: 100%; }
    body{
      margin:0;
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji","Segoe UI Emoji";
      color: var(--text);
      background: radial-gradient(1200px 600px at 30% 110%, rgba(0,140,255,.18), transparent 55%),
                  radial-gradient(900px 500px at 85% 0%, rgba(0,220,255,.10), transparent 55%),
                  linear-gradient(180deg, var(--bg0), var(--bg1));
      display:flex;
      align-items:center;
      justify-content:center;
      padding: 18px;
    }

    .frame{
      width: 100%;
      max-width: 980px;
      border: 1px solid var(--stroke);
      border-radius: calc(var(--radius) + 8px);
      background: linear-gradient(180deg, rgba(255,255,255,.03), rgba(255,255,255,.015));
      box-shadow: 0 30px 80px rgba(0,0,0,.55);
      overflow:hidden;
      position: relative;
    }

    /* Fog layers */
    .frame::before{
      content:"";
      position:absolute;
      inset:-40px;
      background:
        radial-gradient(500px 280px at 18% 70%, rgba(0,180,255,.12), transparent 60%),
        radial-gradient(520px 300px at 55% 30%, rgba(0,120,255,.10), transparent 60%),
        radial-gradient(420px 260px at 90% 75%, rgba(0,220,255,.08), transparent 60%);
      filter: blur(10px);
      opacity: .9;
      pointer-events:none;
    }

    .content{
      position: relative;
      padding: 18px 18px 16px;
    }

    .top{
      display:flex;
      align-items: baseline;
      justify-content: space-between;
      gap: 12px;
      padding: 10px 10px 14px;
    }

    .title{
      font-size: 18px;
      letter-spacing: .06em;
      text-transform: uppercase;
      color: rgba(255,255,255,.86);
      font-weight: 700;
    }

    .tag{
      font-size: 12px;
      color: var(--muted2);
      border: 1px solid var(--stroke);
      background: rgba(255,255,255,.03);
      padding: 6px 10px;
      border-radius: 999px;
      backdrop-filter: blur(8px);
      white-space: nowrap;
    }

    .grid{
      display:grid;
      grid-template-columns: 1fr 1.35fr;
      gap: 14px;
      padding: 0 10px 10px;
    }

    .card{
      border: 1px solid var(--stroke);
      border-radius: var(--radius);
      background: linear-gradient(180deg, var(--card), var(--card2));
      padding: 14px 14px 12px;
      backdrop-filter: blur(10px);
      box-shadow:
        0 12px 40px rgba(0,0,0,.40),
        0 0 0 1px rgba(0,0,0,.25) inset;
    }

    .card.rules{
      box-shadow:
        0 16px 46px rgba(0,0,0,.42),
        0 0 0 1px rgba(0,0,0,.25) inset,
        0 0 60px var(--glow);
    }

    .card .head{
      display:flex;
      align-items:center;
      justify-content: space-between;
      gap: 10px;
      margin-bottom: 10px;
    }

    .label{
      display:flex;
      align-items:center;
      gap: 10px;
      font-weight: 700;
      color: rgba(255,255,255,.90);
    }

    .dot{
      width: 10px;
      height: 10px;
      border-radius: 999px;
      background: rgba(0,200,255,.85);
      box-shadow: 0 0 18px rgba(0,200,255,.45);
      flex: 0 0 auto;
    }

    .dot.amber{
      background: var(--amber);
      box-shadow: 0 0 22px var(--amberGlow);
    }

    .mini{
      font-size: 12px;
      color: var(--muted2);
    }

    ol, ul{
      margin: 0;
      padding-left: 18px;
      color: var(--muted);
      line-height: 1.45;
      font-size: 13.5px;
    }

    li{ margin: 6px 0; }
    strong{ color: rgba(255,255,255,.92); font-weight: 700; }

    .purpose ul{
      list-style: none;
      padding-left: 0;
      margin-top: 6px;
    }

    .pill{
      display:flex;
      align-items:flex-start;
      gap: 10px;
      padding: 8px 10px;
      border: 1px solid rgba(255,255,255,.10);
      background: rgba(0,0,0,.16);
      border-radius: 14px;
      margin: 8px 0;
    }

    .pill .k{
      font-weight: 800;
      letter-spacing: .02em;
      color: rgba(255,255,255,.92);
      min-width: 86px;
    }
    .pill .v{
      color: var(--muted);
    }

    .footer{
      padding: 8px 12px 14px;
      display:flex;
      justify-content: flex-end;
    }

    .quote{
      font-size: 12.5px;
      color: rgba(255,255,255,.70);
      border-left: 2px solid rgba(255,140,0,.65);
      padding: 6px 10px;
      background: rgba(255,140,0,.06);
      border-radius: 10px;
    }

    /* Responsive: stack cards on small embeds */
    @media (max-width: 720px){
      .grid{ grid-template-columns: 1fr; }
      .tag{ display:none; }
      .title{ font-size: 16px; }
    }
  </style>
</head>
<body>
  <div class="frame" role="img" aria-label="Experiment Hub header widget with two panels: rules and purpose">
    <div class="content">
      <div class="top">
        <div class="title">Experiment Hub</div>
        <div class="tag">quiet progress • loud results</div>
      </div>

      <div class="grid">
        <section class="card rules">
          <div class="head">
            <div class="label"><span class="dot"></span>Few rules</div>
            <div class="mini">small, consistent</div>
          </div>
          <ol>
            <li><strong>Small</strong>, consistent iterations</li>
            <li><strong>Track</strong> the result</li>
            <li><strong>Analyze</strong> later</li>
            <li><strong>Regroup</strong> and try again</li>
          </ol>
        </section>

        <section class="card purpose">
          <div class="head">
            <div class="label"><span class="dot amber"></span>What this hub is for</div>
            <div class="mini">signal over noise</div>
          </div>

          <ul>
            <li class="pill">
              <div class="k">Clarity</div>
              <div class="v">Seeing systems accurately.</div>
            </li>
            <li class="pill">
              <div class="k">Influence</div>
              <div class="v">Aligning others faster.</div>
            </li>
            <li class="pill">
              <div class="k">Energy</div>
              <div class="v">Achieving more with less cognitive load.</div>
            </li>
          </ul>

          <div class="footer">
            <div class="quote">If it doesn’t move one of these, it’s noise.</div>
          </div>
        </section>
      </div>
    </div>
  </div>
</body>
</html>
